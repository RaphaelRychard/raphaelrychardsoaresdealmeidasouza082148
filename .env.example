# Perfil
SPRING_PROFILES_ACTIVE=prod

# Postgres
POSTGRES_DB=artist_album
POSTGRES_USER=admin
POSTGRES_PASSWORD=admin123456

# JWT (em prod: usar secret forte e fora do repo)
SECURITY_TOKEN_SECRET=replace-with-strong-secret

# CORS
APP_ALLOWED_ORIGINS=http://localhost:3000,http://localhost:3001,http://127.0.0.1:5500

# MinIO
MINIO_ROOT_USER=minioadmin
MINIO_ROOT_PASSWORD=minioadmin
MINIO_BUCKET_NAME=album-covers
MINIO_PRESIGNED_URL_EXPIRATION=1800

# Rate limit
RATE_LIMIT_REQUESTS=10
RATE_LIMIT_WINDOW=60000